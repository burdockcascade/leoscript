[package]
name = "leoscript"
version = "0.1.0"
edition = "2021"
authors = ["Peter Richmond"]

[features]
default = ["compiler", "runtime"]
compiler = []
runtime = []

[dependencies]
log = "0.4.20"
regex = "1.10.2"

[dev-dependencies]
simplelog = "0.12.1"