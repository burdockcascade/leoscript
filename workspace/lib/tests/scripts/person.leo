class Person

    attribute name
    attribute age

    constructor(name, age)
        self.name = name
        self.age = age
    end

    function getName()
        return self.name
    end

    function getAge()
        return self.age
    end

end