module Math

    function min(a,b)
        return std_math_min(a,b)
    end

    function max(a,b)
        return std_math_max(a,b)
    end

    function abs(a)
        return std_math_abs(a)
    end

    function sqrt(a)
        return std_math_sqrt(a)
    end

end